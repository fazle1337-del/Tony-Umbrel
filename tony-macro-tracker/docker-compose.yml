services:
  db:
    image: postgres:16-alpine
    container_name: macro-tracker-db
    restart: on-failure
    environment:
      POSTGRES_DB: macrotracker
      POSTGRES_USER: macrouser
      POSTGRES_PASSWORD: changeme123
    volumes:
      - ${APP_DATA_DIR}/db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U macrouser -d macrotracker"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    image: YOURDOCKERHUBUSERNAME/macro-tracker-api:latest
    container_name: macro-tracker-api
    restart: on-failure
    environment:
      DATABASE_URL: postgres://macrouser:changeme123@macro-tracker-db:5432/macrotracker
      API_SECRET: myapisecret123
      NODE_ENV: production
    depends_on:
      db:
        condition: service_healthy

  frontend:
    image: YOURDOCKERHUBUSERNAME/macro-tracker-frontend:latest
    container_name: macro-tracker-frontend
    restart: on-failure
    ports:
      - "8069:80"
    depends_on:
      - api
